# Resume UI (Flask)

Simple Flask app that provides a form-based UI and live preview for a resume template.

Quick start:

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5000/
